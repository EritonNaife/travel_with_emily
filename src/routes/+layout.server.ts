/**
 * Its most important job is to check for a user's session cookie on every request.

It makes the current user's data (or null if not logged in) available to all pages. This is how your main header can instantly switch between showing "Login" and "My Account."
 */
